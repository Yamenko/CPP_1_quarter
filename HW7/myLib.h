#pragma once

namespace MyFunc {
	void initArr(float*, const size_t);
	void printArr(float*, const size_t);
	void plusMinusCount(float*, const size_t, int&, int&);
	void userEnterArr(int*, const size_t);
	void sortArr(int*, const size_t);
}